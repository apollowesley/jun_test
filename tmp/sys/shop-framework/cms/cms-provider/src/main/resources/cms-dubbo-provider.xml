<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="
     http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
 	 http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd
 	 http://www.springframework.org/schema/context  http://www.springframework.org/schema/context/spring-context-3.2.xsd
       ">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="cms-provider"  />
    <dubbo:consumer check="false" retries="0"></dubbo:consumer>
    <!-- 使用zookeeper注册中心暴露服务地址 -->
    <dubbo:registry address="${dubbo.zookeeper.address}" protocol="${dubbo.zookeeper.protocol}"/>

    <!--监控中心地址 -->
    <!--<dubbo:monitor protocol="registry" />-->

    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol
            name="${dubbo.protocol.name}"
            port="${dubbo.protocol.port}"
            threadpool="fixed"
            threads="100"
            buffer="8192"
            accepts="1000"
            accesslog="true"
            payload="88388608"
            heartbeat="0" />


    <!--下面写接口的配置-->





    <!-- 品牌服务 -->
    <dubbo:service interface="com.kld.cms.api.ISysUserService" ref="sysUserService"  version="1.0.0" />
    <bean id="sysUserService" class="com.kld.cms.service.impl.SysUserServiceImpl" />

</beans>