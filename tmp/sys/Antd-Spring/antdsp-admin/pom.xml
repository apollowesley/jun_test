<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.antdsp</groupId>
		<artifactId>antdsp</artifactId>
		<version>0.0.1</version>
		<relativePath>../pom.xml</relativePath>
	</parent>

	<artifactId>antdsp-admin</artifactId>
	<name>antdsp-admin</name>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<qiniu.version>7.2.22</qiniu.version>
		<druid.version>1.1.18</druid.version>
		<antdsp.core.version>0.0.1</antdsp.core.version>
		<shiro-spring.version>1.3.2</shiro-spring.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.shiro</groupId>
			<artifactId>shiro-spring</artifactId>
			<version>${shiro-spring.version}</version>
		</dependency>
		<!-- 只需引入spring-boot-devtools 即可实现热部署 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<optional>true</optional>
		</dependency>
		<!-- 七牛云存储 -->
		<dependency>
			<groupId>com.qiniu</groupId>
			<artifactId>qiniu-java-sdk</artifactId>
			<version>${qiniu.version}</version>
		</dependency>
		<!-- Druid -->
		<dependency>
		    <groupId>com.alibaba</groupId>
		    <artifactId>druid</artifactId>
		    <version>${druid.version}</version>
		</dependency>
		<dependency>
			<groupId>com.alibaba</groupId>
			<artifactId>druid-spring-boot-starter</artifactId>
			<version>1.1.13</version>
		</dependency>
		
		<dependency>
			<groupId>com.antdsp</groupId>
			<artifactId>antdsp-core</artifactId>
			<version>${antdsp.core.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<source>1.8</source>
					<target>1.8</target>
					<encoding>UTF-8</encoding>
					<verbal>true</verbal>
					<fork>true</fork> <!-- 如果没有该配置，devtools不会生效 -->
					<!-- 指定该Main Class为全局的唯一入口 -->
					<mainClass>com.antdsp.ApplicationStarter</mainClass>
					<layout>ZIP</layout>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>repackage</goal><!--可以把依赖的包都打包到生成的Jar包中 -->
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<profiles>
		<profile>
			<id>dev</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				<antdsp.db.url>jdbc:mysql://192.168.1.231:3306/antdsp?allowMultiQueries=true&amp;useUnicode=true&amp;characterEncoding=UTF-8&amp;serverTimezone=Asia/Shanghai</antdsp.db.url>
				<antdsp.db.username>root</antdsp.db.username>
				<antdsp.db.password>123456</antdsp.db.password>
				<antdsp.db.driver>com.mysql.cj.jdbc.Driver</antdsp.db.driver>
				<antdsp.db.datasource>com.alibaba.druid.pool.DruidDataSource</antdsp.db.datasource>
				
				<antdsp.logback.filepath>logs</antdsp.logback.filepath>
				
				<qiniu.accessKey>TnmENtV4x4oG0qfnXY3gSAmLplBw9bvRJ3IKS0X1</qiniu.accessKey>
				<qiniu.secretKey>nMKAApnVrmBDifwuT5Pq5V33XhNOgcT4njErx5fV</qiniu.secretKey>
				<qiniu.bucket>antdsp</qiniu.bucket>
			</properties>
			<build>
				<filters>
					<filter>src/main/resources/application.yml</filter>
				</filters>
			</build>
		</profile>
	</profiles>
</project>