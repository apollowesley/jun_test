<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.UserMapper">
    <insert id="addUser" parameterType="pojo.User">
        insert into user values(null,#{username},#{password},null)
    </insert>
    <update id="editUser" parameterType="pojo.User">
        update user set username=#{username},password=#{password} where id=#{id}
    </update>
    <delete id="userDel" parameterType="integer">
        delete from user where id=#{id}
    </delete>
    <select id="getUserByUsername" resultType="pojo.User" parameterType="String">
       select * from user where username=#{username}
    </select>
    <select id="getAllUser" resultType="pojo.User">
       select * from user
    </select>
    <select id="getUserById" resultType="pojo.User" parameterType="integer">
        select * from user where id=#{id}
    </select>
</mapper>