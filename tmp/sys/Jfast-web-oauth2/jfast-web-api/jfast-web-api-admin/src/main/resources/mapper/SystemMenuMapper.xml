<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfast.web.api.admin.mapper.SystemMenuMapper">

    <!-- 根据用户角色获取菜单资源 -->
    <select id="findMenuByAdminId" resultType="map">
       select * from system_menu where (
           select menu_id from system_role_menu where role_id in (
              select role_id from system_admin_role where admin_id = #{adminId}
           )
       ) order by id desc
    </select>

    <!-- tree菜单列表 -->
    <select id="queryList" resultType="map">
        select id as value, name, url, permissions, icon, sort, type, parent_id
        from system_menu where del_flag = 0 order by id desc
    </select>

</mapper>