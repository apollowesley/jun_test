server:
  port: 8001
spring:
  application:
    name: jfast-web-registry
# 注册中心配置
eureka:
  client:
    # 是否将自己注册到注册中心
    register-with-eureka: false
    # 是否从注册中心获取注册信息
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:${server.port}/eureka/


