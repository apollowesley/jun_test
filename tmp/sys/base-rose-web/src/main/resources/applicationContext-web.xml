<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context-3.0.xsd
    http://www.springframework.org/schema/task
    http://www.springframework.org/schema/task/spring-task.xsd
    http://www.springframework.org/schema/mvc
    http://www.springframework.org/schema/mvc/spring-mvc.xsd
    http://www.springframework.org/schema/aop
    http://www.springframework.org/schema/aop/spring-aop.xsd
    http://www.springframework.org/schema/tx
    http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- 开启组件扫描 -->
    <!-- 对包中的所有类进行扫描，以完成Bean创建和自动依赖注入的功能 -->
    <context:component-scan base-package="com.jeasy"/>

    <!-- 定时任务 -->
    <task:annotation-driven/>

    <!-- 开启注解 -->
    <mvc:annotation-driven />

    <!-- 启用AspectJ对Annotation的支持 -->
    <aop:aspectj-autoproxy/>

    <bean id="characterEncodingInterceptor" class="com.jeasy.base.interceptors.CharacterEncodingInterceptor">
        <property name="encoding" value="UTF-8" />
    </bean>

    <bean id="reqTimeCostInterceptor" class="com.jeasy.base.interceptors.ReqTimeCostInterceptor">
        <property name="priority" value="100" />
    </bean>

    <bean id="daoTimeCostLogMethodInterceptor" class="com.jeasy.base.interceptors.SQLTimeCostLogMethodInterceptor" />

    <bean id="daoTimeCostLoggerProcessor" class="com.jeasy.base.interceptors.DaoTimeCostLoggerProcessor">
        <property name="interceptor" ref="daoTimeCostLogMethodInterceptor" />
    </bean>

    <!-- Transaction Support -->
    <tx:advice id="useTxAdvice" transaction-manager="txManager">
        <tx:attributes>
            <tx:method name="remove*" propagation="REQUIRED" read-only="false" rollback-for="java.lang.Exception"/>
            <tx:method name="save*" propagation="REQUIRED" read-only="false" rollback-for="java.lang.Exception" />
            <tx:method name="modify*" propagation="REQUIRED" read-only="false" rollback-for="java.lang.Exception" />

            <tx:method name="find*" propagation="SUPPORTS"/>
            <tx:method name="get*" propagation="SUPPORTS"/>
            <tx:method name="page*" propagation="SUPPORTS"/>
            <tx:method name="count*" propagation="SUPPORTS"/>
        </tx:attributes>
    </tx:advice>

    <!--把事务控制在Service层-->
    <aop:config>
        <aop:pointcut id="pc" expression="execution(public * com.jeasy.*.service.*.*(..))" />
        <aop:advisor pointcut-ref="pc" advice-ref="useTxAdvice" />
    </aop:config>
</beans>