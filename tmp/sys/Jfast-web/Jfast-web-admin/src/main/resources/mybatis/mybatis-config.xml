<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
    PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 打印查询语句 -->
    <settings>
        <setting name="logImpl" value="STDOUT_LOGGING" />
    </settings>

    <typeAliases>
        <!-- 配置modelMap -->
        <typeAlias alias="modelMap" type="com.jfast.common.model.ModelMap" />
    </typeAliases>
    
    <plugins>
        <plugin interceptor="com.jfast.common.pagination.PagingInterceptorAdapter">
            <property name="dialect" value="MySql"/>
        </plugin>
    </plugins>
</configuration>