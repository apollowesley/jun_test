<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="system.log">    <insert id="system.log.save" parameterType="map">        insert into system_log        <foreach collection="params.keys" item="key" open="(" close=")" separator="," >            ${key}        </foreach>        values        <foreach collection="params.values"  item="value" open="(" close=")" separator=",">            #{value}        </foreach>    </insert>    <select id="system.log.list" resultType="map">        select * from system_log        <where>            <if test="startTime != null and createDate != ''">                and create_date  &gt;= #{startTime}            </if>            <if test="endTime != null and endTime != ''">                and create_date &lt;= #{endTime}            </if>        </where>        order by id desc    </select></mapper>