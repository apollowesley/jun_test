<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
	<import resource="classpath:spring/application-service.xml" />
	<dubbo:application name="${dubbo.application.name}" />
	<!--zookeeper注册中心 -->
	<dubbo:registry address="${dubbo.registry.address}"
		group="${dubbo.registry.group}" />
	<!--使用multicast广播注册中心暴露服务地址 -->
	<!--<dubbo:registry address="multicast://192.168.65.128:1234" /> -->
	<dubbo:protocol name="dubbo" port="20881" threads="100" />
	<!-- 配置监控的服务地址和IP address:直连监控地址 protocol：如果为protocol="registry"，表示从注册中心发现监控中心地址，否则直连监控中心。 -->
	<dubbo:monitor address="${dubbo.monitor.address}"
		protocol="${dubbo.monitor.protocol}" />
	<!-- 延迟到Spring初始化完成后，再暴露服务,全局超时设置，服务调用超时设置为20秒,超时不重试 -->
	<dubbo:provider delay="-1" timeout="${dubbo.privoder.timeout}" retries="${dubbo.privoder.retries}" />
	<!-- 发布服务 -->
	<dubbo:service interface="com.bodsite.demo.service.HellowService"
		ref="hellowService" />
	<dubbo:service interface="com.bodsite.demo.service.TradeService"
		ref="tradeService" />
	
</beans>